# Handoff — 11 August 2026

Session closed deliberately: three sessions were independently designing the same issue
tracker and pasting conflicting setups across repos. Eric closed all working sessions,
adjudicated, and will reopen them. **This repo is wrapped up and needs nothing to resume.**

---

## Repo state at close

`main` clean, in sync with `origin/main`. Three commits pushed today:

| Commit | What |
|---|---|
| `8779bef` | CLAUDE.md — the WP subfolder is end-of-life (was written 9 Aug, sat unpushed until today) |
| `67b6027` | All eight CI/release actions bumped to the Node 24 runtime |
| *(this one)* | README + promo-page bug-report address; node20 worksheet rev. 2; this handoff |

Build/release contract verified intact: `local.properties` `sdk.dir` resolves,
`keystore.properties` `storeFile` exists, `release.yml` still produces the stable asset name
`rocket-locator-26.apk`, and the live `v0.02` release carries it.

---

## What this session did

### 1. Node 20 → Node 24: **done for this repo, verified**

`docs/github-actions-node20-bump.md` never covered `rocket_locator`. Both workflows here were
entirely Node 20 and both were already emitting the deprecation annotation — including
`release.yml`, which publishes the APK. When the shim is removed, that failure lands **on the
tag push**, i.e. exactly when you try to ship v0.03.

Bumped in `67b6027`: `checkout` v4→v7, `setup-java` v4→v5, `setup-gradle` v4→v5,
`upload-artifact` v4→**v6**, `action-gh-release` v2→v3.

Verified on run [31554666696](https://github.com/mtnmanak/rocket_locator/actions/runs/31554666696):
`conclusion=success` **and the annotations array is empty** — the annotation being gone is the
real pass signal, not the green check.

**Still unexercised:** `action-gh-release@v3` only runs on a `v*` tag. Its v3 notes say the
change is purely the Node 20→24 runtime move with no input changes, so `files`,
`generate_release_notes` and `body` should behave identically — but that is read, not observed.
**If v0.03 ever fails at the publish step, that is the first line to look at.**

### 2. `docs/github-actions-node20-bump.md` rewritten to rev. 2

Rev. 1 was wrong in five material ways and the other four repos still work from it. Rev. 2
corrects: the scope miss (this repo), `upload-artifact@v5` is **still Node 20** (v6 is the
floor), `configure-pages@v5` is Node 20 (so `deploy-pages.yml` has five affected actions, not
four), `upload-pages-artifact@v4` doesn't fix that file (composite pins a node20 upload-artifact),
and the `C:/git` paths / `gh run watch` procedure are both unrunnable here.

**Only this copy is rev. 2.** The identical file in `bp_calculator`, `diesel_dashboard`,
`motor_dashboard` and `motor_sim` is still rev. 1 and carries all five errors. Copy this one
over them, or work from this one.

Remaining work is four `deploy.yml` files (`bp_calculator` 35/37, `motor_dashboard` 39/41,
`motor_sim` 31/33, `online_open_rocket` 37/39) plus the dormant `deploy-pages.yml`. Each push
is that repo's production deploy.

### 3. Issue tracking — adjudicated elsewhere; this repo's part is done

**The decision lives at `E:\git\mountainmanrockets\docs\issue-tracking-consolidation.md`
(rev. 3, marked ADJUDICATED). Do not re-derive it.** Summary as it affects this repo:

- One public tracker, `mountainmanrockets-feedback`, for the site + four public web tools.
- **`rocket_locator` keeps its own Issues tab.** It is already public with Issues enabled;
  app users arrive from GitHub Releases and Obtainium and are already standing in this repo.
- Do **not** create a feedback repo, labels, issue templates or a labeling workflow here.
  There is no `tool:rocket-locator` label and the app is not in the tracker's dropdown.
- Reversing this later costs one label, one dropdown option, one line in the Action's map.

**This repo's assigned task (§5 step 7) is complete** — it was the one item that needed no
rename, so it was unblocked:
- `README.md:9` — "Found a bug, or want a feature?" → this repo's Issues, with the
  `admin@mountainmanrockets.com` email fallback (preserves the "two doors" convention).
- `site/deploy/index.html:146` — footer now carries a report link.
- `site/deploy/index.html:130` — "Get in touch" was unlinked prose; now points at Issues.

That closes a live defect: the promo page has been asking for bug reports since launch
(`index.html:143`) while giving no address anywhere on the page or in the README.

`CLAUDE.md:44-54` marks only the **WordPress** copy end-of-life; `site/deploy/index.html` is
the source of truth and is safe to edit. It has **not** been re-uploaded to WordPress, per
that same rule.

### 4. Cross-repo audit (the G:\ → E:\ move)

The move did no git damage — no repo is behind origin, no broken remotes, no stranded
worktrees, no tracked secrets. Findings that are **not** in this repo and remain open are
recorded in the adjudication doc's sibling notes; the ones that touch this repo:

- 48 files here still contain `G:\git` paths, all inside **gitignored Gradle state**
  (`.gradle/configuration-cache/*/entry.bin`, `app/build/**`, `transport/build/**`).
  Harmless to git. **But delete `app/build`, `transport/build`, `core/build` and `.gradle`
  before trusting the next release build** — that state was written under the old path and
  no clean build has been run since the move.
- Claude Code project memory was orphaned by the move: 29 files across 8 projects still sit
  under `C:\Users\Eric\.claude\projects\G--git-*\memory\`, and every `E--git-*` equivalent is
  empty. Fix is a directory copy per project.

---

## Open in this repo

| Item | Note |
|---|---|
| `docs/build-plan.md:12` and `:89` are stale | Both still say the repo goes public at first stable release. It went public **7 Jul 2026** (`f96f5c7`) because `releases/latest/download/…` 404s anonymously on a private repo, so the website download button and Obtainium auto-updates could not work. Documented at `site/wordpress/README.md:34-39`. |
| `docs/feedback-2026-08-11a.md:1` has a stale title | Reads "# OpenRocket Online — Notes & Feature Requests". The file was broadcast byte-identical to five repos; the heading belongs to another project. Content is correct. Cite it by line, not title. |
| In-app "Report a problem" button | Not built, deliberately. Nearest home is the `footer_version` stamp at `HomeScreen.kt:115-121`. Per the adjudication this is ~10 lines with `LocalUriHandler` — **no gradle change needed**; `buildConfig = true` is not required if the version comes from `stringResource(R.string.footer_version)`. Note Settings is **Phase 6**, not Phase 5. |
| Version is duplicated | `app/build.gradle.kts:29-30` (`versionCode = 2` / `versionName = "0.02"`) vs `strings.xml:51` (`footer_version` = "v0.02 beta"). Two sources, no check. |

---

## Do not

- Re-open the issue-tracker design question. It is adjudicated; the file says so at the top.
- Create issue templates, labels, or a feedback repo here.
- Re-upload `site/deploy/index.html` to the WordPress subfolder as a matter of course.
- `git add -A` in any sibling repo while doing the Node bumps — several carry unrelated
  untracked files, and `diesel_dashboard/body.txt` is *motor_dashboard's* release note sitting
  in the wrong repo.
